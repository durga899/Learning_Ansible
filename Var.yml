- name: Demo on variables
  hosts: all
  vars:
    URL: playbook.com
    Founder: Rajesh
  tasks:
    - name: Print URL
      ansible.builtin.debug:
        msg:
          - url = {{ URL }}

- name: Demo on file var
  hosts: all
  vars_file:
    - sample_var.yml
  tasks:
    - name: Printing var from file
      ansible.builtin.debug:
        msg: url = {{URL}}